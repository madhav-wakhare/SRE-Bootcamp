student-api was installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".
Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

Call the API:

    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Release.Name }} 8080:{{ .Values.service.port }}
    curl http://localhost:8080/healthcheck

DATABASE_URL comes from Vault via the Secret "{{ .Release.Name }}-db-url". Check that it synced:

    kubectl get externalsecret -n {{ .Release.Namespace }}

If the pod is stuck in Init, the migration container is waiting on the database:

    kubectl logs -n {{ .Release.Namespace }} deploy/{{ .Release.Name }} -c run-migrations

PostgreSQL {{ .Chart.AppVersion }} was installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

In-cluster address:

    {{ include "postgres-db.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Database "{{ .Values.config.POSTGRES_DB }}", user "{{ .Values.config.POSTGRES_USER }}".

{{ if .Values.externalSecrets.enabled -}}
The password comes from Vault ({{ .Values.externalSecrets.remoteKey }} → {{ .Values.externalSecrets.remoteProperty }})
via ClusterSecretStore "{{ .Values.externalSecrets.secretStore }}". Verify the sync — STATUS must be SecretSynced:

    kubectl get externalsecret {{ include "postgres-db.fullname" . }} -n {{ .Release.Namespace }}

The pod stays in CreateContainerConfigError until that Secret exists.
{{- else -}}
The password is read from the existing Secret "{{ .Values.existingSecret.name }}".
{{- end }}

Open a psql shell with:

    kubectl exec -it -n {{ .Release.Namespace }} {{ include "postgres-db.fullname" . }}-0 -- psql -U {{ .Values.config.POSTGRES_USER }} -d {{ .Values.config.POSTGRES_DB }}

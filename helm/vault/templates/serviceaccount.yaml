# Gives the Vault pod a Kubernetes identity. Vault's Kubernetes auth method uses
# this account's token to call the TokenReview API (see rbac.yaml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: vault
    app.kubernetes.io/instance: {{ .Release.Name }}
